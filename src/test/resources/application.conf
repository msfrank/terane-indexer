akka {
  loglevel = DEBUG
  loggers = ["akka.event.slf4j.Slf4jLogger"]
  debug {
    receive = on
    autoreceive = on
    lifecycle = on
    fsm = on
    event-stream = on
    unhandled = on
    router-misconfiguration = on
  }
}
